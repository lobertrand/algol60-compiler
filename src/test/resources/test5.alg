begin 
	if a=b then
		begin
			if a=b then 
				string test;
			if a=>b then 
				a:=b
			else 
				s:=2;

			if a\/b then
				real a ;
			if false then 
			if true then 
				integer x;
			patate:=frite
		end;
	if a=>b then
		begin
			comment by experiment find min and max integer;
			integer n;
			ininteger (0,n);
			outinteger( 1,n)
		end

end
